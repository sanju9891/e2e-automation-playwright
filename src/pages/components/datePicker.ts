import BasePage from '@pages/basePage';
import { Page } from '@playwright/test';

export default class DatePickerComponent extends BasePage {
	constructor(page: Page) {
		super(page);
	}

	/**
	 * Application contents
	 */

	/**
	 * Locators
	 */
}
